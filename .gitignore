# Scratch / temporary
tmp/

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# IDE / editor
.vscode/
.idea/
*.swp
*~

# Internal docs (specs, plans) -- kept local, not published
/docs/

# Pre-Flight memory file -- a per-user-project artifact the coach writes at the
# project root. Appears here only when the coach is run inside its own folder
# during testing; never committed to the coach repo.
PREFLIGHT.md

# Claude Code local config (incl. .claude/skills). Local-only: the shipped,
# cross-surface home for the /learn skill is reference/skills/learn/SKILL.md.
# Copy it into .claude/skills/learn/ locally for native slash registration.
.claude/
